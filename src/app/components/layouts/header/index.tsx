import Image from "next/image";

const Header = () => {
    return (
        <div className="w-full flex lg:flex-row text-white bg-black">
            Header
        </div>
    );
};

export default Header;
